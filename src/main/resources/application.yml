spring:
  profiles: default
  application:
    name: user-profile-service
  data:
    mongodb:
      host: localhost
      port: 27017
      database: assignment
logging:
  level:
    root: INFO
server:
  port: 9090
