spring:
  profiles: default
  application:
    name: user-profile-service
  data:
    mongodb:
      host: localhost
      port: 27017
      database: car-wash
logging:
  level:
    root: INFO
server:
  port: 9090
